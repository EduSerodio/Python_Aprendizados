# INTRODUÇÃO: Usamos para saber quantas vezes um determinado objeto aparece dentro da lista

cores = ["azul", "amarelo", "roxo", "azul"]

cores.count("azul") # 2 vezes
cores.count("amarelo") # 1 vez
cores.count("preto") # 0 vezes

print(cores)